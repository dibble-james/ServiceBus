@model ServiceBus.IServiceBus

@{
    ViewBag.Title = "Index";
}

<h2>Peers</h2>
<ul>
    @foreach (var endpoint in this.Model.Peers)
    {
        <li>@endpoint.PeerAddress</li>
    }
</ul>
<h2>Send Message</h2>
@using (this.Html.BeginForm("sendmessage", "home"))
{
    <br />
    @Html.TextBox("message")
    <button type="submit">Send</button>
}
